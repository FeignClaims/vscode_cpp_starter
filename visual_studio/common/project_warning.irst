.. warning::

  **接下来的内容都只对当前项目有效, 如果创建新的项目则需要重复配置.** 因为每个项目都是且应该是独立的, 其配置自然不应该共享.

  配置了的很多检查都只在编译生成时才会进行, 而 VSCode 是能在写代码的同时检查的. **这似乎有扩展解决了**, 有需要的可以 :menuselection:`扩展` 里搜索 ``analysis``、``on save`` 之类的关键词找找; 我不以 Visual Studio 为主, 就不找了.
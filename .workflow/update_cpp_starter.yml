version: '1.0'
name: pipeline-20230303
displayName: update_cpp_starter
triggers:
  trigger: auto
  push:
    branches:
      precise:
        - master
stages:
  - name: stage-91b39f67
    displayName: update_cpp_starter
    strategy: naturally
    trigger: auto
    executor:
      - FeignClaims
    steps:
      - step: build@python
        name: build_python
        displayName: run update_cpp_starter.py
        pythonVersion: '3.9'
        commands:
          - python3 -m pip install --upgrade pip
          - pip3 config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
          - pip3 install -r requirements.txt
          - python3 script/update_cpp_starter.py
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./
        caches: []
        notify: []
        strategy:
          retry: '0'
